{
  "name": "cubicecho.com",
  "version": "0.1.0",
  "private": true,
  "description": "cubic echo — open-source software",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
